
  # CodeSpeak

  This is a code bundle for CodeSpeak. The original project is available at https://www.figma.com/design/241o8gMXGVzJiAI3vtANdg/CodeSpeak.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  